<!DOCTYPE html>
<html>
    <head>
        <title>Weather</title>
        <link rel="icon" href="/img/weather.png">
        <link rel="stylesheet" href="/css/styles.css">
    </head>
    <body>
        <!--Using hbs, getting values dynamically -->
        <!--<h1>{{title}}</h1> -->
        <!-- Render partials here:
        for that we need 2-cirly braces and a greater than sign between these 2-pairs
        after the > sign, add the partials file name without extension.
        Like below:-->
        <div class="main-content">
            {{>header}}
            <h2>Your weather information at your fingertips.</h2>

            <form>
                <input type="text" placeholder="Location name(ex:Toronto, Canada)">
                <button>Search</button>
            </form>

            <p id="messageOne"></p>
            <p id="messageTwo"></p>
        </div>

        <!--
            old way aithout hbs .
            <h1> The Weather app's home page!! </h1>
        <h1> Served using hbs. </h1> -->
        {{>footer}}

        <script src="js/app.js"></script>
    </body>
</html>